# Reown (WalletConnect) Project ID.
# Get one free at https://cloud.reown.com (Dashboard -> Create project).
# This is a PUBLIC id meant for frontend configuration - it is not a secret.
VITE_REOWN_PROJECT_ID=YOUR_PROJECT_ID
